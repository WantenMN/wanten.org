import { Category } from "@/types";

export const baseTitle = "Wanten";
export const baseDesc =
  "Wanten's personal website showcases his work and thoughts.";
export const CATEGORIES: Category[] = ["thoughts", "works", "notes"] as const;
